node_modules/
.DS_Store
*.log
.vite/
.claude/*
!.claude/commands/

# NOTE: dist/ is intentionally NOT ignored — it's the versioned build that
# gets copied into the backend repo's static/components/ for serving.
